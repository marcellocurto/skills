interface:
  display_name: "Implement to PR"
  short_description: "Build an issue or request and open a PR"
  default_prompt: "Use $implement-to-pr to build this and open a pull request that's ready for review."
policy:
  allow_implicit_invocation: false
